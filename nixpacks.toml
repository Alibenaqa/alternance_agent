[phases.setup]
nixPkgs = ["nss", "nspr", "atk", "cups", "dbus", "expat", "libdrm", "libxkbcommon", "mesa", "libGL", "glib", "pango", "cairo", "alsa-lib", "xorg.libX11", "xorg.libXcomposite", "xorg.libXdamage", "xorg.libXext", "xorg.libXfixes", "xorg.libXrandr", "xorg.libxcb"]

[phases.build]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "playwright install chromium --with-deps && python main.py"
